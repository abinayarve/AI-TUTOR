# AI Teacher — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# LLM providers:
#   - Groq is the recommended (free, high-quota) provider. Get a free
#     key at https://console.groq.com/keys — no credit card needed.
#   - Gemini is supported as a fallback if you already have a key.
# See llm_provider.py for how the two are selected automatically.

# --- Core app ---
streamlit>=1.35

# --- LLM providers (LangChain) ---
langchain-core>=0.2
langchain-groq>=0.1.9        # recommended — free tier
langchain-google-genai>=1.0  # fallback provider

# --- RAG / knowledge grounding ---
langchain-community>=0.2
langchain-text-splitters>=0.2
chromadb>=0.5
sentence-transformers>=2.7   # backs HuggingFaceEmbeddings

# --- Document loaders ---
pypdf>=4.2                   # PyPDFLoader
docx2txt>=0.8                # Docx2txtLoader
python-pptx>=0.6.23          # PPTX ingestion + slide generation
python-docx>=1.1             # Word document output
reportlab>=4.2                # PDF generation (learning reports)

# --- Voice ---
edge-tts>=6.1

# --- Visuals / charts / video frames ---
numpy>=1.26
plotly>=5.22
Pillow>=10.3
matplotlib>=3.9

# --- Data / pydantic ---
pydantic>=2.7

# NOTE: real MP4 video rendering (video_engine.py / frame_renderer.py)
# additionally requires the FFmpeg binary to be installed on the
# system (not a pip package):
#   Ubuntu/Debian:  sudo apt install ffmpeg
#   Streamlit Cloud: add a packages.txt file containing the line
#                     "ffmpeg"
